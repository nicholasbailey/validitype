{
  "name": "validitype",
  "version": "0.0.2",
  "description": "A flexible validation and type checking library for Typescript",
  "main": "lib/index.js",
  "scripts": {
    "test": "npx jest",
    "build": "npx tsc",
    "perpare": "npm run build",
    "prepublishOnly": "npm test",
    "version": "git add -A src",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:nicholasbailey/validitype.git"
  },
  "author": "Nicholas Bailey",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.3",
    "jest-extended": "^0.11.5",
    "ts-jest": "^26.4.4",
    "typescript": "^4.0.5"
  }
}
